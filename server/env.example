# Vue3-Admin 后端服务环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改

# ============================================
# 服务器配置
# ============================================

# 服务器端口（与前端 VITE_PORT 保持一致，或使用 PORT）
VITE_PORT=3300
# 或使用简化命名（向后兼容）
PORT=3300

# ============================================
# JWT 配置
# ============================================

# JWT Access Token 密钥（生产环境必须修改为强随机字符串）
# 支持多种命名方式，优先级：VITE_JWT_* > JWT_* > 简化命名
VITE_JWT_ACCESS_TOKEN_SECRET=art-design-pro-access-token-secret-key-2024
# 或使用 JWT_ 前缀（推荐用于后端）
JWT_ACCESS_TOKEN_SECRET=art-design-pro-access-token-secret-key-2024
# 或使用简化命名（向后兼容）
ACCESS_TOKEN_SECRET=art-design-pro-access-token-secret-key-2024

# JWT Refresh Token 密钥（生产环境必须修改为强随机字符串）
VITE_JWT_REFRESH_TOKEN_SECRET=art-design-pro-refresh-token-secret-key-2024
# 或使用 JWT_ 前缀（推荐用于后端）
JWT_REFRESH_TOKEN_SECRET=art-design-pro-refresh-token-secret-key-2024
# 或使用简化命名（向后兼容）
REFRESH_TOKEN_SECRET=art-design-pro-refresh-token-secret-key-2024

# Access Token 过期时间（默认 2 小时）
VITE_JWT_ACCESS_TOKEN_EXPIRES_IN=2h
# 或使用 JWT_ 前缀
JWT_ACCESS_TOKEN_EXPIRES_IN=2h
# 或使用简化命名
ACCESS_TOKEN_EXPIRES_IN=2h

# Refresh Token 过期时间（默认 7 天）
VITE_JWT_REFRESH_TOKEN_EXPIRES_IN=7d
# 或使用 JWT_ 前缀
JWT_REFRESH_TOKEN_EXPIRES_IN=7d
# 或使用简化命名
REFRESH_TOKEN_EXPIRES_IN=7d

# ============================================
# 说明
# ============================================
# 1. 环境变量支持多种命名方式，代码会按优先级自动选择
# 2. VITE_ 前缀：与前端保持一致，适合前后端共享 .env 文件
# 3. JWT_ 前缀：推荐用于后端专用配置
# 4. 简化命名：向后兼容旧配置
# 5. 生产环境必须修改所有密钥为强随机字符串

